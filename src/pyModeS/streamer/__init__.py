"""Removed in v3 — see :mod:`pyModeS._v2_removed`.

v2's ``pyModeS.streamer`` (beast/modes-live framework) is
replaced in v3 by the :class:`pyModeS.PipeDecoder` class and
the ``modes live`` CLI command.
"""

from pyModeS._v2_removed import v2_removed_error

raise v2_removed_error("pyModeS.streamer")
